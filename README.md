# Image Transformation in MATLAB

This project demonstrates basic image processing operations in MATLAB, including rotation, resizing, and flipping.  
It reads an image, applies transformations, and displays the results side by side.

## 📂 Project Structure
- `2.png` → Input image file (place in your MATLAB project folder).
- `image_transform.m` → MATLAB script containing the code.

## 🖼️ Features
- **Rotation** → Rotate the image by 45 degrees using `imrotate`.
- **Resizing** → Resize the image to 50% of its original size using `imresize`.
- **Flipping** → Flip the image using `flip`.
- **Visualization** → Display all versions (original + transformed) in a single figure using `subplot`.

## 🚀 Usage
1. Clone this repository:
   ```bash
   git clone https://github.com/your-username/image-transform-matlab.git

## Output
<img width="1600" height="900" alt="image" src="https://github.com/user-attachments/assets/89f329e8-97ac-4692-8f8f-1e6e5a51f67f" />
